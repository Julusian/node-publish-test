{
  "name": "@julusian/test-lib",
  "version": "1.0.0",
  "packageManager": "yarn@4.10.3",
  "main": "dist/main.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/Julusian/node-publish-test"
  },
  "scripts": {
    "build": "rimraf dist && run build:main",
    "build:main": "tsc --project tsconfig.build.json",
    "lint": "eslint . --ext .ts",
    "prettier": "prettier --write .",
    "watch-types": "tsc --watch --project tsconfig.json",
    "test": "vitest"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@tsconfig/node20": "^20.1.5",
    "@types/node": "^20.17.50",
    "prettier": "^3.5.3",
    "rimraf": "^6.0.1",
    "typescript": "~5.8.3",
    "vitest": "^3.1.4"
  }
}
